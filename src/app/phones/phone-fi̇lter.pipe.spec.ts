import { PhoneFilterPipe } from './phone-fi̇lter.pipe';

describe('PhoneFİlterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
