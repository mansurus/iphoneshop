import { AccessorySortPipe } from './accessory-sort.pipe';

describe('AccessorySortPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessorySortPipe();
    expect(pipe).toBeTruthy();
  });
});
